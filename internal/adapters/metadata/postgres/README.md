# adapters/metadata/postgres/

PostgreSQL implementation of `ports.MetadataDB`.

## Purpose

Stores vulnerability metadata, scan results, and finding details in PostgreSQL. Supports CRUD operations and severity-based queries.
