# adapters/metadata/

Vulnerability metadata adapter. Implements `ports.MetadataDB`.

## Subdirectories

| Directory              | Description                               |
| ---------------------- | ----------------------------------------- |
| [postgres/](postgres/) | PostgreSQL implementation of `MetadataDB` |

## Purpose

Stores and retrieves structured vulnerability metadata (scan results, findings, severity counts).
