# adapters/sandbox/

Sandbox adapter. Implements `ports.SandboxPort`.

## Purpose

Provides isolated container execution environments for tools that need sandboxing (e.g., code execution, untrusted operations).
