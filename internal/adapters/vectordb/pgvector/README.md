# adapters/vectordb/pgvector/

pgvector implementation of `ports.VectorDB`.

## Purpose

Uses the pgvector PostgreSQL extension for storing and querying vector embeddings. Supports cosine similarity and other distance metrics for RAG pipelines.
