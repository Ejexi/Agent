# adapters/memory/

Generic memory adapter. Implements `ports.MemoryPort`.

> **Note:** `MemoryPort` is deprecated. Prefer purpose-specific ports: `MetadataDB`, `LogDB`, `VectorDB`.

## Subdirectories

| Directory              | Description                               |
| ---------------------- | ----------------------------------------- |
| [postgres/](postgres/) | PostgreSQL implementation of `MemoryPort` |
