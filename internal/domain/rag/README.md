# domain/rag/

Domain types for Retrieval-Augmented Generation (RAG).

## Purpose

Defines entities and value objects for the RAG pipeline — embedding storage, document chunks, and similarity search contracts.

## Rules

- Pure domain types only — no infrastructure dependencies.
- Used by VectorDB ports and adapters.
