# tools/implementations/echo/

Echo tool — simple testing and debugging tool.

## Purpose

Returns the input as-is. Used for verifying Kernel execution, tool registration, and end-to-end message flow.

## Registration

Registered in bootstrap as: `echo.NewEchoTool()`
